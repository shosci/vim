" Double quote leading a comment line

" File type checking, plugin, indent on
filetype plugin indent on

" Smart tab
set shiftwidth=4
set tabstop=4
set smarttab

set number " Show line number

" file name, space separator, file type, file status(modified or readonly),
" center placeholder, column, line/total lines, line percentage, padding space
set statusline=%f\ %y%(%m%r%)%=%c,%l\/%L\ %p%%\ 

" Always display status line
set laststatus=2

" Backspace works in Insert mode (e.g. not inserting a ^?), but won't delete over line breaks, or automatically-inserted indentation, or the place where insert mode started:
set backspace=indent,eol,start

" find file in current directory and subdirectories
" For Linux
" set path=$PWD/**
" For Windows
set path=.\**

